# OdyTools Editor UX

Content authoring and inspection workflows for OdyTools and standalone editors.

## Surfaces

| Surface | Entry | Notes |
|---------|-------|-------|
| **OdyTools AIO** | `dotnet build src/Tools/OdyTools/OdyTools.csproj --framework net9.0` | Holocron-style combined editor `[REPO]` |
| **Standalone editors** | Per-csproj under `src/Tools/OdyTools/` (GFF, DLG, NSS, etc.) | Prefer individual csproj over AIO for narrow tasks `[REPO]` |
| **ConvertKotorGame** | `dotnet build src/Tools/ConvertKotorGame/ConvertKotorGame.csproj --framework net9.0` | K1↔TSL portability wizard `[REPO]` |

## Expected workflow (typical mod author)

1. Open or create a game resource (GFF, DLG, 2DA, etc.) via the relevant editor. `[SYNTH]`
2. Edit with format-aware UI; save back to module/installation layout. `[SYNTH]`
3. Optional: compile NSS via NSSComp; diff installs via KotorDiff. `[REPO]` ([run-tools-reference.md](../50-execution/run-tools-reference.md))

## Verification status

| Claim | Status |
|-------|--------|
| OdyTools + standalones compile on net9.0 | Green `[REPO]` (2026-05-24) |
| OdyToolFAC faction/reputation editor | Green — FAC load/build roundtrips, faction/reputation detail edits, context-menu add/remove actions, and Holocron-style Delete key removal for both selected factions and selected reputations are covered by `OdyToolFACTests`; FAC standalone opens under Xvfb (2026-06-28) `[REPO]` |
| OdyToolNSS NCS disassembly tab | Green — `DisassembleNcsBytes` + tab UI tests `[REPO]` (2026-05-24) |
| Reference finder (script ResRef, UTC/UTD/UTP/UTT/UTE/UTM/ARE/IFO context menus) | Green — override fixture tests `[REPO]` (2026-05-24) |
| Reference search options dialog (override/modules/chitin, module-glob, StrRef NCS scan) | Green — `ReferenceSearchOptionsDialogTests` `[REPO]` (2026-05-24) |
| StrRef find-refs NCS CONSTI cache path | Green — `StrRefReferenceCache` / `IncludeNcsStrRefScan` gating; **2** KotorCLI CLI tests at plan **286** baseline; superseded by **94** NcsConsti + **10** StrRefReferenceHelper rows (plans **324**–**370**) `[REPO]` |
| Script ResRef NCS CONSTS scanner paths | Green — `NcsConstStringScanner` + `(NCS bytecode) offset_<n>` field paths; **3** NcsConstString + **5** FindScriptResRefInNcsBytes tests `[REPO]` (plan **287**, 2026-05-28) |
| Compiled NCS script ResRef installation e2e | Green — `FindScriptReferences_OverrideCompiledNcs_ReturnsNcsBytecodePath`; **1** ReferenceFinder test `[REPO]` (plan **289**, 2026-05-28) |
| KotorCLI find-refs compiled NCS CLI subprocess | Green — `Cli_FindRefs_Script_CompiledNcsInOverride_ExitsZero`; **1** FindRefsCommandCli test `[REPO]` (plan **290**, 2026-05-28) |
| NCS CONSTI StrRef vs 2DA-memory threshold disambiguation | Green — threshold + opcode-context + action-signature + stack-store heuristics; **94** NcsConsti tests `[REPO]` (plans **086**/**095**/**099**/**292**–**294**/**303**/**305**/**307**/**324**–**370**, 2026-06-03) |
| NCS CONSTI control-flow reachability (JZ/JNZ/JMP fork, while-break, subroutine/infinite-loop) | Green — `NcsConstiScanner` forward scan + cache indexing; **94** NcsConsti tests `[REPO]` (plans **324**–**370**, 2026-06-03) |
| KotorCLI find-strref cache-path NCS control-flow gating | Green — in-process + CLI subprocess cache-path tests; **18** FindStrRef + **12** InstallationRefSearch CLI tests `[REPO]` (plans **337**–**338**, 2026-05-24) |
| BioWare find-strref slow vs cache NCS semantics | Green — slow path matches raw CONSTI; cache path excludes dead-path consumers; **3** BioWare FindStrRefReferences CF tests `[REPO]` (plan **339**, 2026-05-24) |
| KotorCLI find-strref slow vs cache documentation | Green — README table + examples under `find-strref` `[REPO]` (plan **340**, 2026-05-24) |
| OdyTools StrRef find-refs cache-path NCS gating | Green — `StrRefReferenceHelper` builds `StrRefReferenceCache` when `IncludeNcsStrRefScan`; **10** StrRefReferenceHelper tests `[REPO]` (plan **346**, 2026-06-03) |
| build-and-test-ladder ref-search filter steps | Green — **Step 3b** NCS CONSTI/StrRef + **Step 3c** ReferenceFinder targeted `dotnet test` filters in [build-and-test-ladder.md](../50-execution/build-and-test-ladder.md) `[REPO]` (plans **348**–**351**, 2026-06-03) |
| 2DA table editor workflow / row **Find References** | Green — load/build, row/column CRUD, copy/paste, sort/filter, find/replace, non-destructive hidden-column build, and `TwoDAMemoryReferenceHelper` row reference lookup are covered by `OdyTool2DATests` (plans 083, 200; 2026-06-28) `[REPO]` |
| Savegame virtual resource detection | Green — base editor load state now detects Holocron-style `SAVEGAME.sav/module.sav/resource.ext` and nested `.sav` virtual paths so derived resource editors can make save-aware choices; covered by `EditorHelpersTests` (2026-06-27) `[REPO]` |
| SAV editor save-info, party, and global-variable workflows | Green — save-info and party-table edits update the live save model, global bool/number/string/location add/remove/edit actions sync immediately to `GlobalVars`, and Add uses unique default names instead of duplicating `NEW_VAR`; covered by `OdyToolSAVTests` (2026-06-28) `[REPO]` |
| OdyToolTPC common image imports and clipboard workflow | Green — PNG/JPG/BMP/TGA/DDS/TPC build-format normalization covered by `OdyToolTPC_CommonBitmapBuildType_UsesWritableTextureFormat`; PNG/JPG imports now build as writable TPC instead of hitting unsupported PNG/JPG writers. The Edit menu Copy/Paste actions now expose commands and roundtrip the active texture through an OdyTools clipboard payload; covered by `OdyToolTPC_CopyPasteMenu_RoundtripsTextureThroughClipboard` and invalid clipboard text coverage (2026-06-28) `[REPO]` |
| BWM walkmesh editor in-window customization | Green — material painting/selection remains live in the editor, and the BWM render/material list palette now uses the same customized Module Designer material colors exposed in Settings while preserving defaults for unmapped surface types; covered by `OdyToolBWMTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| KotorDiff in-app (Tools menu) | Green — shared `KotorDiffApp` host `[REPO]` (2026-05-24) |
| Indoor Map Builder Build/Save/Open | Partial — headless build + reloadable MOD WOK AreaModel tests now cover single-room output, connected-room bidirectional transition remapping, and core MOD metadata resources (`LYT`/`VIS`/`ARE`/`GIT`/`IFO`) whose LYT room entries resolve to generated WOKs; embedded `.indoor` payload reloads from the MOD and the Indoor Builder opens under Xvfb; in-game walkmesh **Unverified** `[OPEN]` |
| Blender/kotorblender detection parity | Green — Blender version parsing accepts Holocron-covered formats such as `Blender 4.2.0 (sub 1)` and `Blender 5.0.0-alpha`, minimum version gating is locked to Blender 3.6+, 4.2+ uses extension install paths while older versions use add-ons, missing kotorblender reports an actionable install status, and stalled `--version` probes now time out without blocking editor startup; covered by `BlenderDetectionTests` (2026-06-28) `[REPO]` |
| Blender IPC launch bridge | Partial — OdyTools can now build and launch the Holocron-style Blender `--python-expr` bridge command with add-on enable attempts for `bl_ext.user_default.io_scene_kotor` and `io_scene_kotor`, configurable IPC port, background loop, installation path, and module path context; Module Designer exposes an `Open in Blender` action that is disabled until a module is loaded, reports missing kotorblender without launching, and forwards the active module path to the bridge. GIT now exposes `Tools -> Open in Blender`, disables it until a GIT file is loaded, reports missing kotorblender without launching, forwards the loaded GIT path to the bridge, and includes the Blender bridge files in `OdyToolGIT.Standalone`. LYT now exposes `Tools -> Open in Blender`, disables it until a LYT file is loaded/saved, reports missing kotorblender without launching, forwards the loaded LYT path to the bridge, and includes the Blender bridge files in `OdyToolLYT.Standalone`. Indoor Builder now exposes an `ActionOpenInBlender` UI hook that is disabled until a map/module path exists, reports missing kotorblender without launching, and forwards the saved/opened indoor path to the bridge. Covered by `BlenderDetectionTests`, `SharedWidgetStartupTests`, `GITEditor_BlenderAction*`, `OdyToolLYT_OpenInBlenderAction*`, `OpenInBlenderAction*`, and Module Designer/GIT/LYT/Indoor Builder standalone Xvfb smokes (2026-06-28). Full live Module Designer/GIT/LYT/Indoor synchronization remains `[OPEN]` |
| ERF/RIM/MOD/SAV/HAK archive editor resource operations | Green — archive add/remove/undo, toolbar selection state, extract conversion helpers, and selected-resource rename now roundtrip through rebuilt ERF bytes; duplicate ResRef/type renames are rejected before they can collapse resources on build; covered by `OdyToolERFTests` (2026-06-28) `[REPO]` |
| Generic GFF tree editor workflow | Green — generic GFF load/build preserves nested structures, generic GFF/RES/GAM fallback routes work without specialized editors, tree removals now rebuild structs from the visible tree so removed labels do not survive in output bytes, and selected ResRef fields trim whitespace/clear invalid values; covered by `OdyToolGFFTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| DLG editor rich tree/link presentation | Green — generated tree headers now render supported bold/color HTML as Avalonia `TextBlock` inlines instead of stripping formatting to plain text, and editable condition/script/sound/voice ResRefs trim whitespace and clear blank/invalid values before build; covered by `OdyToolDLG_FormattedTreeHeader_PreservesBoldAndColorRuns`, `OdyToolDLG_EditablePanelResRefs_TrimAndClearBlankValues`, `OdyToolDLG_EditableResRefHelper_ClearsInvalidValues`, and Xvfb standalone launch (2026-06-28) `[REPO]` |
| GIT area instance editor workflow | Green — instance list, 2D viewport selection/drag, detail-panel edits, type visibility filtering, duplicate/remove, undo/redo, and in-window Add for Creature, Door, Placeable, Trigger, Waypoint, Sound, Store, Encounter, and Camera build back into valid GIT; instance detail ResRefs trim whitespace/clear invalid values, and camera `TemplateResRef` roundtrips through `GITHelpers`; covered by `GFFBasedEditorTests` GIT filters plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| GUI visual editor workflow | Green — tree, preview, and property panel selection remain synchronized; users can add root controls, add child controls, duplicate selected controls, delete selected controls, edit ResRef property fields with trim/invalid-clear normalization, and build the edited control tree back into `.gui`; nested preview hit-testing/selection handles now use the same parent-offset coordinates as rendering; covered by `OdyToolGUITests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| PTH path editor workflow | Green — node list, canvas selection, Add/Connect tool modes, bidirectional edge editing, selected-node coordinate editing, vendor PTH load/build preservation, and Edit-menu Add Node placement from typed coordinates or the last canvas world coordinate are covered by `OdyToolPTHTests`; PTH standalone opens under Xvfb (2026-06-28) `[REPO]` |
| IFO module info editor workflow | Green — structured module fields, script fields, generated tags, entry direction degrees/radians conversion, find-next navigation across tag/VO/hak/entry/script fields, and entry/script/movie ResRef trim/clear behavior are covered by `GFFBasedEditorTests`; IFO standalone opens under Xvfb (2026-06-28) `[REPO]` |
| JRL journal editor workflow | Green — quest/category and entry selection exposes structured detail panels, add/remove/duplicate/move/sort/filter/find workflows build back into valid JRL, and loaded-JRL edits no longer restore the original `Categories` list over user changes while preserving unsupported original metadata; covered by `GFFBasedEditorTests` JRL filters plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| LIP lipsync editor workflow | Green — audio duration import, scrub preview, head-preview mouth labels, playback shortcuts, keyframe add/update/delete build roundtrips, and find-next selection of matching keyframes are covered by `OdyToolLIPTests`; LIP standalone opens under Xvfb (2026-06-28) `[REPO]` |
| SSF soundset editor workflow | Green — structured sound-event rows edit every StrRef slot, find-StrRef navigation selects the matching row and preview panel, the XAML menu exposes installation StrRef reference search, and build/reload/undo/redo preserve all sound slots; covered by `OdyToolSSFTests` (2026-06-28) `[REPO]` |
| UTS sound object editor workflow | Green — structured sound source fields build into valid UTS, TemplateResRef trims whitespace/clears blank values, sound-list add/edit/move/remove uses ResRef-style defaults, trims edited entries, skips blank sound rows on build, and preserves sound order; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTT/BTT trigger editor workflow | Green — structured trigger/trap/script fields build into valid trigger GFF, BTT input preserves a BTT content marker on build, and script ResRef fields trim whitespace/clear blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTM/BTM merchant editor workflow | Green — structured merchant pricing/store/inventory fields build into valid merchant GFF, BTM input preserves a BTM content marker on build, and OnOpen script ResRef trims whitespace/clears blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTP/BTP placeable editor workflow | Green — structured placeable, inventory, lock, and script fields build into valid placeable GFF, BTP input preserves a BTP content marker on build, and script ResRef fields trim whitespace/clear blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTD/BTD door editor workflow | Green — structured door, lock, and script fields build into valid door GFF, BTD input preserves a BTD content marker on build, and script ResRef fields trim whitespace/clear blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTE/BTE encounter editor workflow | Green — structured encounter, spawn, creature-list, and script fields build into valid encounter GFF, BTE input preserves a BTE content marker on build, and script ResRef fields trim whitespace/clear blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTC/BTC/BIC creature editor workflow | Green — structured creature stats, inventory, conversation, and script fields build into valid creature GFF, BTC/BIC input preserves the matching content marker on build, and ResRef fields trim whitespace/clear blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTI/BTI item editor workflow | Green — structured item stats, cost, stack, property-list, and comment fields build into valid item GFF, BTI input preserves a BTI content marker on build, and TemplateResRef trims whitespace/clears blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| UTW waypoint editor workflow | Green — structured waypoint, map-note, tag, ResRef, and comment fields build into valid waypoint GFF, in-window note editing is wired, and TemplateResRef trims whitespace/clears blank values; covered by `GFFBasedEditorTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| LYT layout editor workflow | Green — layout lists stay synchronized with scene/detail selection, imported model selection enables the in-window `Add Model As Room` action, selected room/track/obstacle model ResRefs trim whitespace and fall back to valid defaults for invalid values, and the added room remains selected and builds into the LYT. The `Generate Walkmesh` action now refuses unsaved layouts, writes simple round-trippable AreaModel WOK placeholders next to the loaded LYT for each room, and skips existing WOK files by default; model-geometry-derived walkmesh generation remains out of scope for this action. Covered by `OdyToolLYTTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| MDL model inspector/editor workflow | Green — model metadata edits, tab-scoped node/texture/animation selection details, and selected-animation name/length/transition edits build back into ASCII MDL; covered by `OdyToolMDLTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| LTR editor quick-edit and generator controls | Green — side-panel quick edit updates the selected probability table, visible distribution tools operate on the active mode/context, generator failure is bounded for incomplete distributions, and valid distributions generate samples; covered by `OdyToolLTRTests` and `LTRGenerationTests` (2026-06-28) `[REPO]` |
| TLK talk table search/jump workflow | Green — search and Go To panels now start hidden like Holocron, toggle from the editor actions, and Go To respects the active filtered view instead of selecting hidden source rows; detached headless grids avoid visual scroll calls while live windows still scroll selected entries; covered by `OdyToolTLKTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| TXT editor Notepad-style editing controls | Green — find/replace respects case and whole-word matching without looping on partial matches, replace-all preserves skipped partial words, fallback UI exposes status/zoom/status-bar controls when XAML is unavailable, and word wrap/zoom/status/build behavior is covered by `OdyToolTXTTests` (2026-06-28) `[REPO]` |
| WAV/audio editor workflow | Green — WAV/MP3 plus OGG/WMA/WMV/XMV/FLAC/BMU aliases load, preserve source bytes on build, show format metadata, and create playback temp files with the loaded resource extension for non-magic aliases such as BMU/WMV/XMV; covered by `OdyToolWAVTests` plus Xvfb standalone launch (2026-06-28) `[REPO]` |
| Editor roundtrip fidelity vs original Holocron/PyKotor | **Partial** — default headless load/build and structured field-edit/build coverage now spans UTC, UTD, UTE, UTI, UTM, UTP, UTS, UTT, UTW, ARE, BWM, GIT, GUI, IFO, JRL, LYT, MDL, NSS, PTH, and TLK; broader original-resource parity remains `[OPEN]` |
| Standalone/AIO launch UX on Linux | Green — `helper_scripts/smoke-odytools-standalones.sh` verifies standalone apphosts, unified `OdyTools.Standalone(...)` routes, OdyTools.NET aliases, Holocron-compatible `*-editor` aliases, Module Designer, and Indoor Builder on a fresh Xvfb display; 141 standalone/list/alias checks passed after adding `gui-editor` and `lyt-editor` alias coverage (2026-06-24). `helper_scripts/smoke-odytools-routed-open.sh` verifies 97 real `--open` launch routes: TXT/INI/CFG/LOG/2DA_BAK/TXI/VIS, 2DA/CSV and positional 2DA launch, ARE, BWM, DLG/JSON/CNV/Twine HTML/Twine JSON, FAC, GFF/XML/JSON/RES/GAM, GIT, GUI, IFO, JRL, LIP/XML/JSON, LTR, LYT, MDL/MDX, NSS, PTH, ERF/MOD/RIM/HAK/BIF/SAV, savegame folders containing case-insensitive `savegame.sav`, SSF/XML, TLK/XML/JSON, TPC/TGA/DDS/PNG/JPG/BMP/PLT, UTC/BTC/BIC, UTD/BTD, UTE/BTE, UTI/BTI, UTM/XML/JSON/BTM, UTP/XML/JSON/BTP, UTS/XML, UTT/XML/BTT, UTW, and WAV/MP3/OGG/WMA/FLAC/BMU/WMV/XMV fixtures (2026-06-27). User-reported startup-crash cluster BWM/SAV/SSF/UTI/LYT/PTH/LIP was rebuilt and live-smoked on Xvfb with normal `OdyTool*(No Installation)` windows (2026-06-28). MDL, LTR, and TXT standalones were rebuilt and live-smoked with normal no-installation windows after their in-window workflow updates (2026-06-28) `[REPO]` |
| ARE area editor workflow | Green — structured area fields, map/fog/weather/grass/script controls, generated tags, unsupported-field preservation, and envmap/grass/script ResRef trim/clear behavior are covered by `GFFBasedEditorTests`; ARE standalone opens under Xvfb (2026-06-28) `[REPO]` |
| Standalone per-editor installation selection | Green — `File -> Settings` is exposed across editors and the standalone installation bar wraps every editor. The selected game path now refreshes installation-backed editor state for ARE, UTD, UTE, UTI, UTM, UTP, UTT, and UTW; `EditorSettingsMenuTests` plus an Xvfb `OdyTools.Standalone --editor utd --game-path ...` launch verified this path (2026-06-24) `[REPO]` |
| Per-editor documentation/help | Green — mapped editors now auto-expose a single `Help -> Documentation` action with `F1` hotkey after their menus are built, existing `_Help` headers are reused instead of duplicating menus, and every mapped wiki filename must exist or be provided inline; covered by `EditorSettingsMenuTests` (2026-06-28) `[REPO]` |

## Test coverage

- `tests/OdyTools.Tests/` covers selected editor behaviors (2DA table load/build, row/column CRUD, hidden-column non-destructive build, DLG rich tree-header formatting and field roundtrips, ERF archive add/remove/rename/extract helpers, GFF nested load/build and tree remove/rebuild behavior, GIT instance add/edit/duplicate/delete/undo workflows, GUI visual control add/duplicate/delete/edit workflows, ARE structured fields and ResRef trim/clear behavior, IFO structured fields, find-next navigation, and ResRef trim/clear behavior, JRL loaded-resource edit preservation plus quest/entry manipulation workflows, LIP audio/keyframe/find workflows, MDL selectable inspector, tab-scoped details, metadata edit/build, and animation metadata edit/build, FAC context-menu and Delete-key faction/reputation operations, BWM material selection/painting/customized palette, PTH node/edge/canvas-tool/add-coordinate workflows, LYT imported-model add/select workflow, LTR quick-edit/distribution/generator controls, SAV save-info/party/global-vars editing, SSF soundset row editing/find/undo, UTS ResRef and sound-list add/edit/move/remove/build behavior, UTT/BTT trigger build-format and script-field behavior, UTM/BTM merchant build-format and script-field behavior, UTP/BTP placeable build-format and script-field behavior, UTD/BTD door build-format and script-field behavior, UTE/BTE encounter build-format and script-field behavior, UTC/BTC/BIC creature build-format and script-field behavior, UTI/BTI item build-format and ResRef behavior, UTW waypoint edit/build and ResRef behavior, TLK hidden search/jump filtering behavior, TXT find/replace/view controls, WAV/audio alias loading and playback temp-file handling, NSS source edit/analyze/disassembly, reference finder, indoor map, TPC image import/build format selection and clipboard copy/paste, UTC/UT*/ARE/BWM/GIT/GUI/IFO/JRL/LYT/NSS/PTH/TLK load-build and structured field-edit roundtrips, etc.). `[REPO]`
- No browser/automation suite for Avalonia UX in CI. `[REPO]`

## Repo implications

- Format correctness bugs → BioWare parsers + editor code under `OdyTools/`.
- Prefer standalone editor csproj when debugging a single format surface.
